/**
 * 豆包即梦（Seedream）图像生成 - 二期预留
 * 环境变量：VOLC_ACCESS_KEY、VOLC_SECRET_KEY
 * 模型：doubao-seedream-4-0-250828、seedream-3.0
 */

export async function generateImageWithSeedream(
  prompt: string,
  options?: { size?: string; n?: number }
): Promise<string[]> {
  // 二期实现
  throw new Error("AI 图像生成功能二期预留");
}
